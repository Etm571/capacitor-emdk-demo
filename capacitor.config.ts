import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etm571.capacitor.emdk.demo',
  appName: 'capacitor-emdk-demo',
  webDir: 'dist'
};

export default config;
